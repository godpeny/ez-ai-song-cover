# Binaries for Apple Silicon

## FFMPEG, FFPROBE 
**Build for Apple arm64 architecture. [[link](https://github.com/godpeny/ffmpeg-apple-arm64-build)]** 

better putting bins in `/usr/local/bin` or `/usr/bin` since ``RVC`` is using them.
